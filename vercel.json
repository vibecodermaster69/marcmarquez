{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "comment": [
    "No crons here on purpose. The deployment is a READER: a serverless",
    "filesystem is read-only, so it cannot ingest results.",
    "Ingest runs in the GitHub Actions 'sync' workflow, which commits the",
    "updated phoenix93.db back to the repository. That push redeploys this",
    "site, so the page is refreshed by the same commit that fetched the race."
  ],
  "github": { "silent": true }
}
